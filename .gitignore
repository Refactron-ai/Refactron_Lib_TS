# Worktrees
.worktrees/

# Build output
dist/
*.js.map
*.d.ts.map

# Dependencies
node_modules/

# Test coverage (vitest v8 output at repo root only — anchored so it does
# not collide with src/analyze/coverage/ or other in-tree modules)
/coverage/

# Session data
.refactron/

# Personal Claude Code overrides — settings.json (team) is committed,
# settings.local.json (personal) is not. Same pattern as .vscode.
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
YRC/

# Python
__pycache__/
*.pyc
.pytest_cache/
bench/*-loc/

# Python wrapper build artifacts (refactron-py)
refactron-py/dist/
refactron-py/build/
refactron-py/**/*.egg-info/

refactron-py/*.whl
refactron-py/*.tar.gz
*.tgz
