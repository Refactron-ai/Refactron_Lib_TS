{
  "name": "ts-legacy-mini",
  "version": "0.0.0",
  "private": true,
  "description": "Legacy TypeScript (CommonJS) fixture for Refactron's Week 4 golden e2e test. Do not modify.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
